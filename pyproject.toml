[project]
name = "calculator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "neutromax", email = "bhavithnanilkar6@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "rich>=13.0.0",
]

[project.scripts]
calculator = "calculator:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
